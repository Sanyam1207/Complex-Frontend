import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["i.pinimg.com", "res.cloudinary.com", "lh3.googleusercontent.com"], // Add the domain here
  },
};

export default nextConfig;
